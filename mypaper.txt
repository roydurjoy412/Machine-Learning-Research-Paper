% ============================================
% ACM CONFERENCE TEMPLATE (SIGCONF) - GOLD STANDARD
% ============================================
\documentclass[sigconf]{acmart}
% ============================================
% PACKAGES 
% ============================================
\usepackage{amsmath}   % for equations
\emergencystretch=1em

% ============================================
% COPYRIGHT & CONFERENCE INFO
% ============================================
\setcopyright{acmlicensed}
\copyrightyear{2026}
\acmYear{2026}
\acmDOI{10.1145/xxxxxxx.xxxxxxx}   % Replace with actual DOI
\acmConference[ICCA 2026]{4th International Conference on Computing Advancements}{October 15--16, 2026}{Dhaka, Bangladesh}
\acmISBN{978-1-4503-XXXX-X/18/06}   % Replace with actual ISBN

% ============================================
% BEGIN DOCUMENT (Must be placed here)
% ============================================
\begin{document}

% ============================================
% TITLE AND AUTHORS
% ============================================
\title{Leakage-Free Imbalanced Learning for Cardiotocography-Based Fetal Health Classification}

\author{Durjoy Roy}
\affiliation{%
  \institution{American International University-Bangladesh (AIUB)}
  \city{Dhaka}
  \country{Bangladesh}
}
\email{roy.idurjoy@gmail.com}   % Note: Verify if your university uses @student.aiub.edu

\author{Seemanta Torafdar}
\affiliation{%
  \institution{American International University-Bangladesh (AIUB)}
  \city{Dhaka}
  \country{Bangladesh}
}
\email{seemantatarofdar@gmail.com}

\author{Md. Jobarul Islam}
\affiliation{%
  \institution{American International University-Bangladesh (AIUB)}
  \city{Dhaka}
  \country{Bangladesh}
}
\email{22-49971-3@student.aiub.edu}

% ============================================
% ABSTRACT 
% ============================================
\begin{abstract}
Cardiotocography (CTG) is the primary intrapartum fetal monitoring tool, yet manual interpretation is prone to inter-observer variability. This study develops a leakage-free machine learning pipel[...]
\end{abstract}

% ============================================
% CCS CONCEPTS (ACM CLASSIFICATION)
% ============================================
\begin{CCSXML}
<ccs2012>
   <concept>
       <concept_id>10010147.10010257.10010258.10010259.10010263</concept_id>
       <concept_desc>Computing methodologies~Supervised learning by classification</concept_desc>
       <concept_significance>500</concept_significance>
   </concept>
   <concept>
       <concept_id>10010405.10010444.10010449</concept_id>
       <concept_desc>Applied computing~Health informatics</concept_desc>
       <concept_significance>500</concept_significance>
   </concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Computing methodologies~Supervised learning by classification}
\ccsdesc[500]{Applied computing~Health informatics}

% ============================================
% KEYWORDS
% ============================================
\keywords{Cardiotocography; fetal health classification; class imbalance; SMOTE; Random Forest; macro recall; SHAP; calibration}

% ============================================
% MAKETITLE (Must be placed after Abstract & CCS)
% ============================================
\maketitle

% ============================================
% 1. INTRODUCTION
% ============================================
\section{Introduction}
\label{sec:introduction}

Adverse perinatal outcomes remain a major clinical concern, and continuous intrapartum monitoring is routinely used to detect early signs of fetal compromise. Cardiotocography (CTG), which jointly[...]

Developing reliable machine learning (ML) models for CTG-based fetal health classification is nevertheless non-trivial, primarily due to severe class imbalance. In the widely used UCI CTG dataset[...]

A second methodological challenge is data leakage in resampling-based pipelines. Oversampling methods such as the Synthetic Minority Oversampling Technique (SMOTE) \cite{chawla2002} are widely us[...]

In this work, we present a leakage-free, statistically supported, and interpretable ML pipeline for three-class fetal health classification from CTG features. Using a scikit-learn \cite{pedregosa[...]

% ============================================
% 2. RELATED WORK
% ============================================
\section{Related Work}
\label{sec:related_work}

Machine learning approaches for cardiotocography (CTG)-based fetal health classification have been actively investigated, largely using supervised models trained on CTG-derived (engineered) featu[...]

Nazli et al.~\cite{nazli2025} performed a comprehensive comparison across multiple machine learning models on a CTG dataset with 21 features, applying SMOTE to mitigate imbalance and reporting st[...]

Das et al.~\cite{das2023} proposed a framework that distinguishes CTG dynamics across different stages of labor and applied SMOTE only on training data to reduce leakage risk, reflecting strong m[...]

Rahmayanti et al.~\cite{rahmayanti2022} conducted grid-search-based optimization and reported class-wise precision, recall, and F1-scores, supporting the importance of class-level reporting in th[...]

Mushtaq and Veningston~\cite{mushtaq2024} compared classical baselines with deep learning approaches and reported very high overall performance. While these results indicate the potential of comp[...]

Ensemble-based approaches such as those proposed by Kuzu and Santur~\cite{kuzu2023} can deliver high performance, but complex stacked architectures may limit clinical auditability unless supporte[...]

Overall, prior work establishes strong feasibility for CTG-based fetal health classification but leaves open a practical need for pipelines that jointly emphasize (i) leakage-free imbalance corre[...]

\subsection{Research Gaps and Motivation}
\label{subsec:research_gaps}

A synthesis of prior research reveals three persistent and clinically consequential gaps that motivate this study. First, many studies continue to rely on overall accuracy or aggregate measures a[...]

\subsection{Contributions}
\label{subsec:contributions}

To address the aforementioned gaps, the primary contributions of this study are as follows:
\begin{itemize}
    \item \textbf{Leakage-Free SMOTE Pipeline:} We propose a strictly leakage-free imbalanced-learning pipeline for CTG classification in which \texttt{StandardScaler} and SMOTE are applied exclu[...]
    \item \textbf{Macro Recall Optimization:} We perform macro-recall-driven model selection using \texttt{GridSearchCV} under 10-fold stratified cross-validation, comparing seven widely used cla[...]
    \item \textbf{Statistical Validation:} We report reliability and statistical evidence through 95\% confidence intervals and paired Wilcoxon signed-rank tests on cross-validation scores (e.g.,[...]
    \item \textbf{SHAP Explainability:} We provide model interpretability using SHAP \texttt{TreeExplainer}, highlighting dominant predictors for the Pathological class (e.g., \texttt{abnormal\_\allowbreak short\_\allowbreak term\_\allowbreak variability}, \texttt{histogram\_\allowbreak mean}, and \texttt{prolongued\_\allowbreak decelerations}), thereby supporting transparent clinical interpretation.
\end{itemize}
% ============================================
% ============================================
% 3. MATERIALS AND METHODS
% ============================================
\section{Materials and Methods}
\label{sec:methods}

% -------------------------------------------------
\subsection{Dataset and Problem Definition}
\label{subsec:dataset}

This study used the publicly available UCI Cardiotocography (CTG) dataset. The dataset contained zero missing values. After removing duplicate records, the final cohort comprised 2,113 instances [...]

% -------------------------------------------------
\subsection{Train--Test Split and Cross-Validation Protocol}
\label{subsec:split}

To obtain an unbiased final evaluation, the dataset was partitioned into training and test sets using an \textbf{80/20 split} (\texttt{test\_size = 0.20}) with \textbf{stratification by the targe[...]

Within the training set, candidate models were evaluated using \textbf{10-fold stratified cross-validation}. Due to severe class imbalance and the clinical importance of minority-class sensitivit[...]

% -------------------------------------------------
\subsection{Leakage-Free Preprocessing and Resampling Pipeline}
\label{subsec:pipeline}

A leakage-free pipeline was implemented to ensure that performance estimates reflect true generalization. All preprocessing and resampling steps were encapsulated within a single pipeline that wa[...]

\begin{enumerate}
    \item \textbf{StandardScaler} for feature normalization,
    \item \textbf{SMOTE (Synthetic Minority Oversampling Technique)} \cite{chawla2002} to address class imbalance, and
    \item the downstream classifier.
\end{enumerate}

Importantly, \textbf{SMOTE was applied strictly within each training fold}. This prevents information from the validation fold from influencing the training data through synthetic sample generati[...]

For a given class $c$, recall is defined as:
\begin{equation}
\label{eq:recall}
\text{Recall}_c = \frac{TP_c}{TP_c + FN_c}
\end{equation}

Macro-averaged recall is computed as:
\begin{equation}
\label{eq:macro_recall}
\text{Recall}_{macro} = \frac{1}{K} \sum_{c=1}^{K} \text{Recall}_c
\end{equation}

For completeness, precision for class $c$ is defined as:
\begin{equation}
\label{eq:precision}
\text{Precision}_c = \frac{TP_c}{TP_c + FP_c}
\end{equation}

The F1-score for class $c$ is the harmonic mean of precision and recall:
\begin{equation}
\label{eq:f1}
\text{F1}_c = \frac{2 \cdot \text{Precision}_c \cdot \text{Recall}_c}{\text{Precision}_c + \text{Recall}_c}
\end{equation}

Macro-averaged F1-score is:
\begin{equation}
\label{eq:macro_f1}
\text{F1}_{macro} = \frac{1}{K} \sum_{c=1}^{K} \text{F1}_c
\end{equation}

% -------------------------------------------------
\subsection{Models Compared}
\label{subsec:models}

Seven supervised learning approaches were evaluated to cover a diverse set of modeling paradigms:

\begin{itemize}
    \item \textbf{k-Nearest Neighbors (k-NN):} An instance-based learning algorithm that classifies samples based on Euclidean distance to the nearest neighbors in the feature space.
    \item \textbf{Logistic Regression (multinomial):} A linear classifier employing a multinomial formulation, optimized using the \texttt{lbfgs} solver to estimate class probabilities.
    \item \textbf{Decision Tree (DT):} A nonlinear, rule-based classifier that recursively partitions the feature space into decision regions.
    \item \textbf{Gaussian Naïve Bayes (NB):} A probabilistic generative model that assumes feature independence and Gaussian-distributed features within each class.
    \item \textbf{Multi-Layer Perceptron (MLP):} A feed-forward artificial neural network trained using backpropagation, with architectures defined by tuned hidden-layer configurations.
    \item \textbf{Soft-voting ensemble:} A probability-based ensemble method that combines predictions from Logistic Regression, Decision Tree, and Random Forest using soft voting.
    \item \textbf{Random Forest (RF):} An ensemble of decision trees trained via bootstrap aggregation (bagging), configured with \texttt{class\_weight = "balanced"} to mitigate class imbalance \cite{breiman2001}.
\end{itemize}

% -------------------------------------------------
\subsection{Hyperparameter Optimization}
\label{subsec:hyperparams}

Hyperparameter tuning was performed on the training set using \textbf{GridSearchCV} under the same
\textbf{10-fold stratified cross-validation} strategy described previously, with
\textbf{macro-averaged recall} as the optimization criterion
(\texttt{scoring=\allowbreak"recall\_\allowbreak macro"}). Parameter grids were defined at the pipeline level
(with the classifier step named \texttt{clf}) as follows:

\begin{itemize}
    \item \textbf{k-Nearest Neighbors:}
    \texttt{clf\_\_\allowbreak n\_\allowbreak neighbors} $\in \{3, 5, 7, 9\}$.

    \item \textbf{Logistic Regression:}
    \texttt{clf\_\_\allowbreak C} $\in \{0.1, 1, 10\}$, with
    \texttt{max\_\allowbreak iter=2000},
    \texttt{solver=\allowbreak"lbfgs"}, and
    \texttt{multi\_\allowbreak class=\allowbreak"multinomial"}.

    \item \textbf{Decision Tree:}
    \texttt{clf\_\_\allowbreak max\_\allowbreak depth} $\in \{5, 10, \text{None}\}$, with
    \texttt{random\_\allowbreak state=42}.

    \item \textbf{Random Forest:}
    \texttt{clf\_\_\allowbreak n\_\allowbreak estimators} $\in \{100, 200, 300\}$ and
    \texttt{clf\_\_\allowbreak max\_\allowbreak depth} $\in \{5, 10, \text{None}\}$, with
    \texttt{class\_\allowbreak weight=\allowbreak"balanced"}.

    \item \textbf{Multi-Layer Perceptron (MLP):}
    \texttt{clf\_\_\allowbreak hidden\_\allowbreak layer\_\allowbreak sizes} $\in \{(50,), (100,), (100, 50)\}$, with
    \texttt{max\_\allowbreak iter=1000},
    \texttt{early\_\allowbreak stopping=\allowbreak True}, and
    \texttt{random\_\allowbreak state=42}.
\end{itemize}

Gaussian Naïve Bayes was evaluated as an \textbf{untuned baseline}. For each model, the best-performing configuration selected by GridSearchCV corresponds to the complete pipeline (feature scali[...]

% -------------------------------------------------
\subsection{Held-Out Test Set Evaluation}
\label{subsec:test_eval}

Following cross-validated tuning and model selection, the \textbf{best estimator for each classifier} was evaluated on the held-out test set. Performance was summarized using \textbf{accuracy}, \[...]

Accuracy is defined as:
\begin{equation}
\label{eq:accuracy}
\text{Accuracy} = \frac{\sum_{c=1}^{K} TP_c}{N}
\end{equation}
where $TP_c$ denotes the number of correctly classified samples for class $c$, and $N$ is the total number of test samples.

Macro-averaged recall (repeated for completeness) is:
\begin{equation}
\text{Recall}_{macro} = \frac{1}{K} \sum_{c=1}^{K} \frac{TP_c}{TP_c + FN_c}
\end{equation}

Macro-averaged F1-score is:
\begin{equation}
\text{F1}_{macro} = \frac{1}{K} \sum_{c=1}^{K}
\frac{2 \cdot TP_c}{2 \cdot TP_c + FP_c + FN_c}
\end{equation}

Confusion matrix analysis was additionally used to examine class-wise prediction errors and misclassification patterns across Normal, Suspect, and Pathological classes. This held-out evaluation w[...]

% -------------------------------------------------
\subsection{Uncertainty Quantification and Statistical Testing}
\label{subsec:uncertainty}

To report uncertainty beyond point estimates, performance metrics were accompanied by \textbf{95\% confidence intervals (CIs)}. For test-set macro recall, uncertainty was quantified using \textbf[...]

Let $\hat{R}_b$ denote the macro recall computed on the $b$-th bootstrap sample. The bootstrap estimate of the metric distribution is:
\begin{equation}
\label{eq:bootstrap}
\{ \hat{R}_1, \hat{R}_2, \dots, \hat{R}_B \}, \quad B = 1000
\end{equation}

The \textbf{95\% bootstrap confidence interval} is obtained using the percentile method:
\begin{equation}
\label{eq:bootstrap_ci}
\text{CI}_{95\%} = \left[ \hat{R}_{(2.5\%)}, \; \hat{R}_{(97.5\%)} \right]
\end{equation}
where $\hat{R}_{(2.5\%)}$ and $\hat{R}_{(97.5\%)}$ denote the 2.5th and 97.5th percentiles of the bootstrap distribution.

\subsubsection{Statistical Significance Testing}
\label{subsubsec:wilcoxon}

To assess whether observed performance differences were statistically meaningful, paired
\textbf{Wilcoxon signed-rank tests} (see \cite{wilcoxon1945}) were conducted on fold-level
cross\allowbreak-validation macro\allowbreak-recall scores.

Let $d_i = R_i^{(A)} - R_i^{(B)}$ denote the difference in macro recall between two models $A$ and $B$ on the $i$-th fold. The Wilcoxon test evaluates whether the median of the paired differences[...]
\begin{equation}
\label{eq:wilcoxon_h0}
H_0: \text{median}(d_i) = 0
\end{equation}

The test statistic is computed by ranking the absolute differences $|d_i|$ and summing the signed ranks:
\begin{equation}
\label{eq:wilcoxon_w}
W = \sum_{i=1}^{n} \text{sgn}(d_i) \cdot \text{rank}(|d_i|)
\end{equation}
where $\text{sgn}(d_i)$ is the sign of $d_i$ and $n$ is the number of folds.

The resulting $p$-value is compared against the significance level $\alpha = 0.05$ to determine statistical significance. Comparisons focused on the champion model (\textbf{Random Forest}) agains[...]

% -------------------------------------------------
\subsection{Model Explainability}
\label{subsec:shap}

To support interpretability, feature-level explanations were obtained for the best-performing model using \textbf{SHAP (SHapley Additive exPlanations)} with \textbf{TreeExplainer} for Random Fore[...]

For a given input sample $x$, the SHAP value $\phi_j$ represents the contribution of feature $j$ to the model prediction relative to a baseline. The prediction can be expressed as:
\begin{equation}
\label{eq:shap}
f(x) = \phi_0 + \sum_{j=1}^{M} \phi_j
\end{equation}
where $\phi_0$ is the expected model output (baseline) and $\phi_j$ is the contribution of feature $j$.

Global feature importance for the \textbf{Pathological} class was computed by aggregating the mean absolute SHAP values across all samples:
\begin{equation}
\label{eq:shap_importance}
\text{Importance}_j = \frac{1}{N} \sum_{i=1}^{N} \left| \phi_j^{(i)} \right|
\end{equation}
where $\phi_j^{(i)}$ denotes the SHAP value of feature $j$ for the $i$-th sample, and $N$ is the number of samples.

The dominant predictors identified were \texttt{abnormal\_short\_term\_variability}, \texttt{histogram\_mean}, and \texttt{prolongued\_decelerations}, which are consistent with clinically meaning[...]
% ============================================
% 4. RESULTS
% ============================================
\section{Results}
\label{sec:results}

% -------------------------------------------------
\subsection{Cross-Validation Performance}
\label{subsec:cv}

Table~\ref{tab:cv} presents the 10-fold stratified cross-validation macro recall scores for all seven classifiers. Random Forest achieved the highest mean macro recall of 0.9014 (90.14\%), with a[...]

\begin{table}[ht]
\centering
\caption{Cross-Validation Macro Recall (10-fold Stratified CV)}
\label{tab:cv}
\begin{tabular}{lcc}
\toprule
\textbf{Model} & \textbf{Mean} & \textbf{Std} \\
\midrule
Random Forest        & 0.9014 & 0.040 \\
Soft Voting Ensemble & 0.8842 & 0.042 \\
MLP                  & 0.8650 & 0.051 \\
Decision Tree        & 0.8520 & 0.055 \\
Logistic Regression  & 0.8310 & 0.048 \\
k-NN                 & 0.8250 & 0.050 \\
Na\"{i}ve Bayes      & 0.7910 & 0.060 \\
\bottomrule
\end{tabular}
\end{table}

% -------------------------------------------------
\subsection{Statistical Significance Testing}
\label{subsec:significance}

Table~\ref{tab:wilcoxon} presents the results of paired Wilcoxon signed-rank tests comparing Random Forest against the two closest competitors. RF significantly outperformed MLP ($p = 0.009766$, [...]

\begin{table}[ht]
\centering
\caption{Paired Wilcoxon Signed-Rank Test Results}
\label{tab:wilcoxon}
\begin{tabular}{lccc}
\toprule
\textbf{Comparison} & \textbf{$\alpha$} & \textbf{$p$-value} & \textbf{Significant} \\
\midrule
RF vs.\ MLP          & 0.05 & 0.009766 & Yes \\
RF vs.\ Soft Voting  & 0.05 & 0.130859 & No  \\
\bottomrule
\end{tabular}
\end{table}

% -------------------------------------------------
\subsection{Random Forest Confidence Interval}
\label{subsec:rf_ci}

The cross-validated macro recall for the best Random Forest configuration was 90.14\% (95\% CI: 87.25\%--93.04\%), with best hyperparameters \texttt{n\_estimators = 200} and \texttt{max\_depth = [...]

% -------------------------------------------------
\subsection{Held-Out Test Set Performance}
\label{subsec:test_perf}

Table~\ref{tab:test} summarises the held-out test set results for the best Random Forest estimator. RF attained 94.56\% accuracy, 88.87\% macro recall (bootstrap 95\% CI: 83.99\%--93.54\%), and 9[...]

\begin{table}[ht]
\centering
\caption{Held-Out Test Set Performance (Random Forest)}
\label{tab:test}
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{Value} & \textbf{95\% CI} \\
\midrule
Accuracy      & 0.9456 & ---                 \\
Macro Recall  & 0.8887 & 0.8399\,--\,0.9354  \\
Macro F1      & 0.9011 & ---                 \\
\bottomrule
\end{tabular}
\end{table}

% -------------------------------------------------
\subsection{Calibration Analysis}
\label{subsec:calibration}

For clinical decision support, predicted probabilities should be not only discriminative but also
well-calibrated, meaning that a predicted probability should correspond to the empirical frequency of
the outcome. Figure~\ref{fig:calibration} presents one-vs-rest reliability diagrams for the Random Forest
model on the held-out test set for all three classes. The dashed diagonal represents perfect calibration.

Overall, the curves track the diagonal more closely at higher predicted probabilities, indicating that
high-confidence predictions are comparatively reliable. Deviations at lower-to-mid probability bins suggest
residual miscalibration, which can occur under class imbalance and limited minority-class sample sizes.
These calibration results complement the class-balanced performance metrics reported in Table~\ref{tab:test}
and support interpreting the model's probability outputs in addition to hard class labels.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.48\textwidth]{picture/fig_calibration_curves_.png}
  \caption{Calibration curves (reliability diagrams) for the Random Forest model on the held-out test set,
  shown for all classes in a one-vs-rest formulation. The dashed line denotes perfect calibration.}
  \Description{Reliability diagram plotting predicted probability versus observed frequency for Normal,
  Suspect, and Pathological classes. A dashed diagonal line indicates perfect calibration.}
  \label{fig:calibration}
\end{figure}

% -------------------------------------------------
\subsection{Confusion Matrix Analysis}
\label{subsec:cm}

Figure~\ref{fig:cm} presents confusion matrices for all seven classifiers on the held-out test set. Random Forest achieved the strongest overall pattern, correctly classifying 324 of 330 Normal s[...]

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{picture/fig_confusion_matrices.png}
\caption{Confusion matrices for all seven classifiers on the held-out test set (Normal, Suspect, Pathological). RF achieves the best Pathological recall with only 2 missed detections. NB shows th[...]
\Description{Seven confusion matrix plots for k-NN, Logistic Regression, Decision Tree, Naïve Bayes, MLP, Random Forest, and soft-voting ensemble. Random Forest shows the highest diagonal counts[...]
\label{fig:cm}
\end{figure*}

% -------------------------------------------------
\subsection{Learning Curve}
\label{subsec:lc}

Figure~\ref{fig:lc} shows the Random Forest learning curve, plotting training and cross-validated macro recall as a function of training set size. The training recall remains at 1.0 throughout, i[...]

\begin{figure}[ht]
\centering
\includegraphics[width=0.45\textwidth]{picture/fig_learning_curve.png}
\caption{Learning curve for Random Forest (macro recall). Training recall (blue) remains at 1.0 throughout; validation recall (orange) converges to $\approx$0.90 at full training size, with a nar[...]
\Description{Line plot with shaded confidence intervals. Training macro recall stays constant at 1.0. Validation macro recall rises from 0.75 to 0.90 as training set size increases from 150 to 1,[...]
\label{fig:lc}
\end{figure}

% -------------------------------------------------
\subsection{Feature Importance}
\label{subsec:fi}

Figure~\ref{fig:fi} presents Gini-based feature importance for the Decision Tree and Random Forest classifiers.
For the Decision Tree, \texttt{histogram\_\allowbreak mean} (0.25) and
\texttt{mean\_\allowbreak value\_\allowbreak of\_\allowbreak short\_\allowbreak term\_\allowbreak variability} (0.23)
are the two dominant splits, together accounting for nearly half the total impurity reduction.
For Random Forest, importance is more distributed across features, with
\texttt{abnormal\_\allowbreak short\_\allowbreak term\_\allowbreak variability} (0.125) and
\texttt{percentage\_\allowbreak of\_\allowbreak time\_\allowbreak with\_\allowbreak abnormal\_\allowbreak long\_\allowbreak term\_\allowbreak variability} (0.120)
ranked first and second, followed by \texttt{histogram\_\allowbreak mean} (0.105).
The broader distribution of importance in RF relative to DT reflects the ensemble's ability to exploit a wider
range of discriminative features, reducing reliance on any single split and contributing to its superior
generalization performance.

\begin{figure*}[t]
\centering
\includegraphics[width=\textwidth]{picture/fig_feature_importance.png}
\caption{Gini-based feature importance for Decision Tree (left) and Random Forest (right), showing the top-10 features by mean impurity decrease. RF distributes importance more evenly across feat[...]
\Description{Two horizontal bar charts. Left: Decision Tree – histogram\_mean (0.25), mean\_value\_of\_short\_term\_variability (0.23). Right: Random Forest – abnormal\_short\_term\_variabili[...]
\label{fig:fi}
\end{figure*}

% -------------------------------------------------
\subsection{SHAP Explainability --- Pathological Class}
\label{subsec:shap_path}

Figure~\ref{fig:shap_path} presents the SHAP beeswarm summary for the \textbf{Pathological} class.
The most influential predictor is \texttt{histogram\_\allowbreak mean}, where low feature values (blue) are
associated with strongly positive SHAP values, pushing predictions toward Pathological.
\texttt{histogram\_\allowbreak median} and
\texttt{abnormal\_\allowbreak short\_\allowbreak term\_\allowbreak variability}
rank second and third, respectively; for
\texttt{abnormal\_\allowbreak short\_\allowbreak term\_\allowbreak variability}, high feature values (red/pink)
drive large positive SHAP contributions, consistent with the clinical interpretation that elevated short-term
variability abnormality is a hallmark of fetal distress.
\texttt{prolongued\_\allowbreak decelerations} also contributes positively at high values, reflecting the known
association between prolonged heart-rate decelerations and fetal hypoxia.
The remaining features exhibit SHAP distributions clustered near zero, indicating limited marginal contribution
to Pathological-class predictions.

\begin{figure}[t]
\centering
\includegraphics[width=0.45\textwidth]{picture/fig_shap_pathological.png}
\caption{SHAP beeswarm summary for the Pathological class (Random Forest). Each point is one sample; colour encodes feature value (red = high, blue = low). Features are ranked by mean $|\phi_j|$. Low \texttt{histogram\_\allowbreak mean} and high \texttt{abnormal\_\allowbreak short\_\allowbreak term\_\allowbreak variability} are the dominant drivers of Pathological predictions.}
\Description{Beeswarm plot for Pathological class. Top features: histogram\_mean (low values positive SHAP), histogram\_median, abnormal\_short\_term\_variability (high values positive SHAP). SHAP magnitudes up to 0.8.}
\label{fig:shap_path}
\end{figure}

% -------------------------------------------------
\subsection{SHAP Explainability --- Suspect Class}
\label{subsec:shap_susp}

Figure~\ref{fig:shap_susp} presents the corresponding SHAP beeswarm summary for the \textbf{Suspect} class. \texttt{accelerations} is the top-ranked feature: low acceleration counts (blue) are associa[...]

\begin{figure}[ht]
\centering
\includegraphics[width=0.45\textwidth]{picture/fig_shap_suspect.png}
\caption{SHAP beeswarm summary for the Suspect class (Random Forest). \texttt{accelerations} is the dominant predictor: low values push predictions toward Suspect. SHAP magnitudes are smaller than for[...]
\Description{Beeswarm plot for Suspect class. Top features: accelerations (low values positive SHAP, magnitude ~0.2), percentage\_of\_time\_with\_abnormal\_long\_term\_variability (high values positiv[...]
\label{fig:shap_susp}
\end{figure}

% ============================================
% 5. DISCUSSION
% ============================================
\section{Discussion}
\label{sec:discussion}

The results indicate that the proposed leakage-free imbalanced-learning pipeline can achieve strong class-balanced performance for CTG-based fetal health classification. As shown in Table~\ref{ta[...]

Random Forest is particularly well-suited for this task because CTG features often exhibit complex nonlinear relationships, such as interactions between variability measures, deceleration pattern[...]

A key methodological contribution of this work is the strict prevention of data leakage during preprocessing and resampling. Performing oversampling prior to cross-validation splitting can allow [...]

Beyond predictive performance, interpretability is essential for clinical applicability and user trust. Global SHAP analysis (see Figure~\ref{fig:shap_path}) identified \texttt{abnormal\_short\_term\_[...]

\subsection{Limitations}
\label{subsec:limitations}
This study has several limitations. First, the evaluation was conducted on a single retrospective dataset with engineered features rather than raw cardiotocography (CTG) waveforms; therefore, the[...]

% ============================================
% 6. CONCLUSION
% ============================================
\section{Conclusion}
\label{sec:conclusion}

This paper presented a leakage-free and explainable machine learning pipeline for imbalanced three-class fetal health classification using cardiotocography (CTG) features. By applying feature sta[...]

Among the evaluated models, Random Forest achieved the highest cross-validated macro recall and demonstrated strong recall for the clinically critical Pathological class on the held-out test set.[...]

% ============================================
% REFERENCES
% ============================================
\clearpage  
\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

\end{document}
